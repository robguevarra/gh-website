import { UserDetailLoading } from '@/components/admin';

/**
 * Loading state for the user detail page
 * This is automatically used by Next.js when the page is loading
 */
export default function UserDetailLoadingPage() {
  return <UserDetailLoading />;
}
