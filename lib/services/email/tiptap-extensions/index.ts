/**
 * Email Components Library for TipTap
 * 
 * This file exports all the email-optimized TipTap extensions
 * for easy imports throughout the application.
 */

import EmailButton from './email-button';
import { EmailColumns, EmailColumn } from './email-columns';
import EmailDivider from './email-divider';
import EmailCard from './email-card';
import EmailImage from './email-image';
import EmailSpacing from './email-spacing';
import EmailHeader from './email-header';
import EmailFooter from './email-footer';

// Export individual components
export {
  EmailButton,
  EmailColumns,
  EmailColumn,
  EmailDivider,
  EmailCard,
  EmailImage,
  EmailSpacing,
  EmailHeader,
  EmailFooter,
};

// Export default collection with all components
export default {
  EmailButton,
  EmailColumns,
  EmailColumn,
  EmailDivider,
  EmailCard,
  EmailImage,
  EmailSpacing,
  EmailHeader,
  EmailFooter,
};
